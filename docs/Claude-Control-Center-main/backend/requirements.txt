flask==3.1.3
flask-cors==6.0.2
beautifulsoup4==4.14.3
python-frontmatter==1.1.0
watchdog==6.0.0
orjson==3.11.8
python-dotenv==1.2.2
httpx==0.28.1
tomlkit==0.14.0

# Agentic OS — Phase 1 (RAG foundation)
lightrag-hku>=1.3.0,<2.0
sentence-transformers>=3.0,<5.0
anthropic>=0.40,<2.0
openai>=1.50,<3.0
google-generativeai>=0.8,<1.0
numpy>=1.26,<3.0
networkx>=3.2,<4.0

# Agentic OS — Phase 1 (Gemini + plan tracking)
# watchdog already listed above

# Agentic OS — Phase 3 (Research pipeline)
praw>=7.7,<8.0
yt-dlp>=2024.1.1
firecrawl-py>=1.0,<2.0

# Agentic OS — News hub (safe RSS/XML parsing; guards against XXE / billion-laughs)
defusedxml>=0.7,<0.8

# Scheduled Automation — in-app cron scheduler (issue #24)
croniter>=2.0,<7.0
