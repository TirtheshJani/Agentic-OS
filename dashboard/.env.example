# AGENTIC_TOKEN (optional) - when set, /api/* rejects requests that aren't
# same-origin unless they include `x-agentic-token: <value>`. Used for
# server-to-server calls if you ever expose the dashboard beyond localhost.
# AGENTIC_TOKEN=replace-with-a-long-random-string
